NODE_ENV=development

# Tenant identity — switch this with `npm run tenant:dev -- --slug=<x>`
# (it overwrites these in the spawned process). Keeping them here so a
# bare `node server.js` also works.
TENANT_SLUG=mbao
TENANT_NAME=Mbao
TENANT_BRAND_KEY=MBAO

SESSION_SECRET=dev-only-do-not-use-in-prod

# Local Postgres — the user created `maas_db` for testing.
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=bonea2024
DB_NAME=maas_db
DB_SSL=false

# Optional integrations — leave blank for tenant testing.
# NOTE: server.js:7249 currently does `throw` if BICTORYS_API_KEY is empty,
# which contradicts this comment. Placeholder below lets local boot succeed;
# fix the hard throw in server.js to match the intent.
BICTORYS_API_KEY=dev-placeholder-not-used
BICTORYS_BASE_URL=https://api.bictorys.com
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Centre de découpe Mata — intégration commandes_decoupe.
# Le serveur Mata local doit tourner sur 3001 et avoir cette clé enregistrée.
MATA_DECOUPE_BASE_URL=http://localhost:3001
MATA_DECOUPE_API_KEY=maas-mbao-30042026
MATA_DECOUPE_CENTRE=Centre de Découpe Banlieue;Centre de Découpe Dakar

# Tracabilite viande — Maas App lit le lot du jour depuis le service DATA.
# Cf lib/data-tracabilite-client.js et la route /api/tracabilite-lot.
DATA_API_BASE_URL=http://localhost:3001
WEB_ORDERS_API_KEY=b326e72b67a9b508c88270b9954c5ca1
DATA_API_KEY=22195f78f78dd5ff038cdb78053cf8d8c1f2e273258996b935c10058addee7ae

# Mata Depenses Management (MataBanq) — source officielle de la creance CDB.
# Cf lib/depenses-creance-client.js et GET /api/finance/creances.
DEPENSES_API_BASE_URL=https://mata-depenses-management.onrender.com
DEPENSES_API_KEY=4f8d9a2b6c7e8f1a3b5c9d0e2f4g6h7i
PORT=3005
MAAS_KEY_API=test-maas-key-api-local
